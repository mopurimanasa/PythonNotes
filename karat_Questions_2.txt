Longest Substring Without Repeating Characters: Return length of the longest substring with unique chars.
Valid Parentheses: Check if brackets in string are valid and properly nested.
Evaluate Reverse Polish Notation: Evaluate expression tokens like ["2","1","+","3","*"]
Merge Intervals: Given intervals [[s,e],...], merge all overlapping intervals.
Insert Interval: Insert new_interval into sorted non-overlapping intervals and merge if needed.
Group Anagrams: Group words that are anagrams of each other.
Top K Frequent Elements: Return k most frequent elements in nums.

Product of Array Except Self: Return array where ans[i] = product(nums) except nums[i], no division.

--Subarray Sum Equals K: Count subarrays whose sum equals k.

--Longest Consecutive Sequence: Return length of longest run of consecutive integers.

Kth Largest Element in an Array: Return kth largest element.

Rotate Array: Rotate nums to the right by k steps (in-place).

Search in Rotated Sorted Array: Return index of target in rotated sorted nums, else -1.

Find Peak Element: Return any peak index where nums[i] > neighbors.

Minimum in Rotated Sorted Array: Return the minimum element in rotated sorted nums (no duplicates).

Meeting Rooms II: Given meeting intervals, return minimum rooms required.

--Daily Temperatures: For each day, return days until a warmer temperature.

--Coin Change: Given coins and amount, return minimum coins needed, else -1.

--House Robber: Max sum of non-adjacent numbers.

Longest Increasing Subsequence (LIS) length.

--Number of Islands: Count connected components of 1s in a grid (4-directional).

Flood Fill: Recolor connected pixels starting at (sr, sc). 

--Course Schedule: Given numCourses and prerequisites [a,b], check if you can finish all courses. - difficult

LRU Cache: Implement get/put with LRU eviction.  -> Revise later
Find Duplicate Number: nums has n+1 integers in [1..n] with one duplicate; find it without modifying array.
Decode String: Decode patterns like "3[a2[c]]" -> "accaccacc".
Flatten Dictionary: Convert nested dict into a flat dict with dot-separated keys.
Merge K Sorted Lists (arrays): Merge k sorted lists into one sorted list.
Generate All Permutations of a String (unique characters)