Python Libraries:

-> A Python library is a collection of pre-written code (functions, classes, modules) that helps us perform specific tasks without writing everything from
scratch.
Why Use Libraries?
-> Save development time
-> Reduce code complexity
-> Reuse tested code
-> Perform complex tasks easily

Common Python Libraries

1. Math Library:
-> Used for mathematical operations.
-> The math module provides a wide range of mathematical functions for many different scientific and engineering applications, including the 
following:

a) Number Functions:

i) math.ceil(): The math.ceil() method maps a floating-point number to the smallest succeeding integer
p = 10.1
print(math.ceil(p)) = 11

ii) math.floor(): The math.floor() method maps a floating-point number to the greatest preceeding integer:
q = 9.99
print(math.floor(q)) = 9

iii) math.factorial(): The math.factorial(n) method returns the product of all natural numbers less than or equal to n, if n a positive integer.
However, if n = 0, it returns 1. The code below uses the math.factorial() to calculate $5!$
n = 5
print("{}! = {}".format(n, math.factorial(n))) = 5! = 120

iv) math.gcd(): The math.gcd() method returns the greatest common denominator for two numbers; we can use it to reduce fractions.
gcd = math.gcd(25, 120)
print(gcd) = 5

v) math.fabs(): The math.fabs() method removes the negative sign of a given number, if any, and returns its absolute value as a float:
print(math.fabs(-25)) = 25.0

b) Power and Logarithmic Functions:

i) math.pow(): The math.pow() method returns a floating-point value representing the value of x to the power of y $(x^y)$.
-> Let's try the math.pow() method by forecasting an investment. To do that, we need to know the initial deposit, the annual interest rate, 
and the number of years that you invest your money in an investment account. Finally, by using the following formula, we can calculate the 
final amount of the investment:
${amount} = deposit(1+interest)^{years}$
-> For example, consider the following values:
    Initial deposit: $10,000
    Annual interest rate: 4%
    Number of years: 5
-> The code calculates the final amount deposited in the investment account after five years:
deposit = 10000
interest_rate = 0.04
number_years = 5
final_amount = deposit * math.pow(1 + interest_rate, number_years)
print("The final amount after five years is", final_amount)
The final amount after five years is 12166.529024000001
-> The math.pow(x, y) method always returns a floating-point value, Let’s check the data type of the function’s return value:
print(type(math.pow(2, 4))) = <class 'float'>

ii) math.exp(): math.exp(x) returns e^x (e raised to the power x).
e = 2.71828...
import math
print(math.exp(2)) = 7.38905609893065

iii) math.sqrt(): The math.sqrt() method returns the square root of a number.
import math
print(math.sqrt(25))
Output: 5.0

iv) math.log(): math.log() is used to calculate the logarithm of a number.
import math
print(math.log(100)) = 4.605170185988092

c) Trigonometric Functions: sin(), cos(), tan() 

import math
angle = math.radians(30)  # Convert degrees to radians
print(math.sin(angle))  # 0.5
print(math.cos(angle))  # 0.866
print(math.tan(angle))  # 0.577

2. Random Library:
-> Used to generate random values.
import random
print(random.randint(1, 10))
Output: 7

3. Datetime Library
-> Used to work with dates and times.
import datetime
print(datetime.datetime.now())

4. OS Library
-> Used for interacting with the operating system.
import os
print(os.getcwd())

5. CSV Library
-> Used for reading and writing CSV files.
import csv
-> Common in data processing projects.

6. JSON Library
-> Used to work with JSON data (widely used in APIs).
import json

Example:
import json
data = '{"name":"Manasa"}'
result = json.loads(data)
print(result["name"])
Output: Manasa

7. Pandas Library
-> Used for data analysis and handling Excel/CSV files.
import pandas as pd
Example: 
import pandas as pd
df = pd.read_csv("employees.csv")
print(df)

8. NumPy Library
-> Used for numerical computations and arrays.
import numpy as np

9. Matplotlib Library
-> import matplotlib.pyplot as plt

10. Requests Library
-> Used for calling APIs and websites.
import requests
Example: 
response = requests.get("https://example.com")
print(response.status_code)




